# Backend API base URL (no trailing slash)
# Dev: Vite proxy forwards /api → localhost:5000, so keep as-is
# Prod: set to your deployed Render/Railway backend URL
VITE_API_URL=https://owmp-ai-platform-backend.onrender.com/api
# Production example: VITE_API_URL=https://owms-api.onrender.com
